machine:
  node:
    version: 6.6.0

dependencies:
  cache_directories:
    - ./node_modules/

test:
  override:
    - npm run lint
    - npm test

deployment:
  production:
    branch: master
    commands:
      - npm run publish:prod
